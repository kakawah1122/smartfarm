# 健康页面 SCSS 变量使用指南

> 统一的设计令牌系统，提升代码可维护性和设计一致性

**创建时间**：2025-10-30  
**适用文件**：`miniprogram/pages/health/health.scss`

---

## 📐 变量系统

### 间距变量（Spacing）
```scss
$spacing-xs: 8rpx;     // 极小间距 - 用于紧密元素间距
$spacing-sm: 12rpx;    // 小间距 - 用于相关元素分组
$spacing-base: 16rpx;  // 基础间距 - 默认间距
$spacing-md: 20rpx;    // 中间距 - 用于卡片内部
$spacing-lg: 24rpx;    // 大间距 - 用于主要内容区域
$spacing-xl: 32rpx;    // 超大间距 - 用于重要分隔
$spacing-2xl: 40rpx;   // 特大间距 - 用于章节分隔
```

**使用场景**：
- `padding`, `margin`, `gap` 等间距属性
- 元素之间的水平/垂直间距

**示例**：
```scss
// ❌ 旧写法（魔术数字）
.card {
  padding: 24rpx;
  margin: 0 16rpx 16rpx;
  gap: 12rpx;
}

// ✅ 新写法（使用变量）
.card {
  padding: $spacing-lg;
  margin: 0 $spacing-base $spacing-base;
  gap: $spacing-sm;
}
```

---

### 圆角变量（Border Radius）
```scss
$radius-xs: 4rpx;      // 极小圆角 - 用于按钮、标签
$radius-sm: 8rpx;      // 小圆角 - 用于输入框
$radius-base: 12rpx;   // 基础圆角 - 默认圆角
$radius-md: 16rpx;     // 中圆角 - 用于卡片
$radius-lg: 24rpx;     // 大圆角 - 用于主要卡片
$radius-xl: 32rpx;     // 超大圆角 - 用于特殊容器
$radius-round: 50%;    // 圆形 - 用于头像、图标
```

**使用场景**：
- `border-radius` 属性
- 卡片、按钮、输入框等圆角设置

**示例**：
```scss
// ❌ 旧写法
.card {
  border-radius: 16rpx;
}
.button {
  border-radius: 24rpx;
}

// ✅ 新写法
.card {
  border-radius: $radius-md;
}
.button {
  border-radius: $radius-lg;
}
```

---

### 字号变量（Font Size）
```scss
$font-xs: 20rpx;       // 极小字号 - 用于辅助信息
$font-sm: 24rpx;       // 小字号 - 用于次要文本
$font-base: 28rpx;     // 基础字号 - 正文文本
$font-md: 30rpx;       // 中字号 - 强调文本
$font-lg: 32rpx;       // 大字号 - 标题文本
$font-xl: 36rpx;       // 超大字号 - 主标题
$font-2xl: 40rpx;      // 特大字号 - 数据展示
$font-3xl: 48rpx;      // 巨大字号 - 关键数据
```

**使用场景**：
- `font-size` 属性
- 文本层级区分

**示例**：
```scss
// ❌ 旧写法
.label {
  font-size: 28rpx;
}
.title {
  font-size: 32rpx;
}
.value {
  font-size: 48rpx;
}

// ✅ 新写法
.label {
  font-size: $font-base;
}
.title {
  font-size: $font-lg;
}
.value {
  font-size: $font-3xl;
}
```

---

### 阴影变量（Box Shadow）
```scss
// 基础阴影
$shadow-sm: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);    // 小阴影
$shadow-base: 0 2rpx 12rpx rgba(0, 0, 0, 0.06); // 基础阴影
$shadow-md: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);   // 中阴影
$shadow-lg: 0 8rpx 24rpx rgba(0, 0, 0, 0.12);   // 大阴影

// 品牌色阴影（用于按钮、强调元素）
$shadow-brand-sm: 0 2rpx 8rpx rgba(0, 82, 217, 0.15);
$shadow-brand-base: 0 4rpx 12rpx rgba(0, 82, 217, 0.25);
$shadow-brand-md: 0 6rpx 16rpx rgba(0, 82, 217, 0.3);
```

**使用场景**：
- `box-shadow` 属性
- 卡片悬浮效果
- 按钮阴影

**示例**：
```scss
// ❌ 旧写法
.card {
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.06);
}
.button {
  box-shadow: 0 4rpx 12rpx rgba(0, 82, 217, 0.25);
}

// ✅ 新写法
.card {
  box-shadow: $shadow-base;
}
.button {
  box-shadow: $shadow-brand-base;
}
```

---

## 🎨 已应用示例

### 1. 批次筛选卡片
```scss
.filter-tabs-card {
  margin: 0 $spacing-lg $spacing-base;     // ✅ 使用间距变量
  border-radius: $radius-md;               // ✅ 使用圆角变量
  box-shadow: $shadow-base;                // ✅ 使用阴影变量
}
```

### 2. 批次筛选区域
```scss
.batch-filter-section {
  padding: $spacing-lg;                    // ✅ 使用间距变量
  
  .current-batch {
    gap: $spacing-sm;                      // ✅ 使用间距变量
    
    .label {
      font-size: $font-base;               // ✅ 使用字号变量
    }
    
    .value {
      font-size: $font-lg;                 // ✅ 使用字号变量
    }
  }
}
```

### 3. 操作按钮
```scss
.filter-action {
  gap: $spacing-xs;                        // ✅ 使用间距变量
  padding: $spacing-sm $spacing-lg;        // ✅ 使用间距变量
  border-radius: $radius-lg;               // ✅ 使用圆角变量
  box-shadow: $shadow-brand-base;          // ✅ 使用品牌阴影变量
}
```

---

## 📊 变量使用统计

**文件信息**：
- 总行数：1,871行
- 已替换：约50处（演示性替换）
- 待替换：约300处（可逐步优化）

**高频数值**（替换优先级）：
1. `16rpx` - 71次 → 使用 `$spacing-base` 或 `$radius-md`
2. `24rpx` - 65次 → 使用 `$spacing-lg` 或 `$radius-lg`
3. `12rpx` - 50次 → 使用 `$spacing-sm` 或 `$radius-base`
4. `8rpx` - 50次 → 使用 `$spacing-xs` 或 `$radius-sm`
5. `32rpx` - 23次 → 使用 `$spacing-xl` 或 `$font-lg`

---

## 🔄 渐进式迁移策略

### 阶段1：核心区域（✅ 已完成）
- ✅ 批次筛选卡片
- ✅ 健康数据概览
- ✅ 操作按钮

### 阶段2：主要内容区域（建议）
- [ ] 统计卡片（`.stat-card`）
- [ ] Tab页面内容区域
- [ ] 预防记录列表
- [ ] 治疗记录列表

### 阶段3：细节区域（可选）
- [ ] 弹窗组件
- [ ] 表单元素
- [ ] 提示信息
- [ ] 其他辅助元素

**执行方式**：
1. 使用编辑器的查找替换功能
2. 每次替换一个数值（如 `16rpx`）
3. 根据上下文选择合适的变量
4. 测试验证样式正确性

**查找替换示例**：
```bash
# 全局查找
padding: 24rpx
margin: 24rpx
gap: 24rpx

# 替换为
padding: $spacing-lg
margin: $spacing-lg
gap: $spacing-lg
```

---

## ✨ 优势和收益

### 1. 一致性提升
- 所有间距统一为标准值
- 字号层级清晰
- 圆角规格统一

### 2. 可维护性提升
- 修改一处，全局生效
- 减少魔术数字
- 代码语义化

### 3. 设计系统化
- 符合TDesign设计规范
- 易于扩展新主题
- 支持深色模式等

### 4. 开发效率提升
- 无需记忆具体数值
- 变量名语义明确
- 降低出错概率

---

## 📝 最佳实践

### DO（推荐）✅
```scss
// 使用变量
.card {
  padding: $spacing-lg;
  border-radius: $radius-md;
  box-shadow: $shadow-base;
  font-size: $font-base;
}

// 变量组合
.button {
  padding: $spacing-sm $spacing-lg;  // 上下12rpx，左右24rpx
}
```

### DON'T（不推荐）❌
```scss
// 硬编码数值
.card {
  padding: 24rpx;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.06);
  font-size: 28rpx;
}

// 非标准数值（除非有特殊需求）
.special {
  padding: 27rpx;  // 不符合设计系统
}
```

### 特殊情况处理
```scss
// 如果确实需要非标准值，添加注释说明原因
.special-layout {
  padding: 18rpx;  // 特殊设计需求：与UI设计稿一致
}
```

---

## 🎯 后续优化建议

### 短期（1-2天）
1. 替换高频数值（16rpx, 24rpx, 12rpx）
2. 统一卡片和按钮样式
3. 统一字号层级

### 中期（1周）
4. 创建公共样式类（如 `.spacing-lg`, `.radius-md`）
5. 提取颜色变量系统
6. 建立响应式断点系统

### 长期（持续）
7. 建立完整的设计令牌系统
8. 支持主题切换（深色模式）
9. 与其他页面的样式系统统一

---

## 🛠️ 工具和命令

### VS Code 快捷替换
1. 打开 `health.scss`
2. 按 `Ctrl+H`（或 `Cmd+H`）打开查找替换
3. 输入查找内容：`padding: 24rpx`
4. 输入替换内容：`padding: $spacing-lg`
5. 逐个检查并替换（或批量替换）

### 正则表达式查找
```regex
# 查找所有 24rpx
\b24rpx\b

# 查找所有 padding/margin 后面的数值
(padding|margin):\s*(\d+rpx)
```

---

## 📚 参考资源

- **TDesign 设计规范**：[https://tdesign.tencent.com/](https://tdesign.tencent.com/)
- **微信小程序 rpx 单位说明**：[微信官方文档](https://developers.weixin.qq.com/miniprogram/dev/framework/view/wxss.html)
- **SCSS 变量文档**：[Sass 官方文档](https://sass-lang.com/documentation/variables)

---

**优化完成时间**：2025-10-30  
**变量系统创建**：完成  
**示范性应用**：完成  
**后续迁移**：可渐进式进行

