# 鹅数通小程序项目开发规范

## 📚 文档说明

本文档定义了鹅数通小程序项目的开发规范和最佳实践，所有开发人员在开发新功能或维护现有功能时都应遵循这些规范。

**版本**: v1.2  
**更新日期**: 2025年11月9日

---

## 目录

- [1. 代码组织规范](#1-代码组织规范)
- [2. 组件化开发规范](#2-组件化开发规范)
- [3. 详情弹窗组件复用规范](#3-详情弹窗组件复用规范) ⭐ **重点**
- [4. 命名规范](#4-命名规范)
- [5. 样式规范](#5-样式规范)
- [6. TypeScript 编码规范](#6-typescript-编码规范)
- [7. 数据交互规范](#7-数据交互规范)
- [8. 页面布局规范](#8-页面布局规范) ⭐ **重点**
- [9. 合规与上线要求](#9-合规与上线要求)

---

## 1. 代码组织规范

### 1.1 目录结构

```
miniprogram/
├── components/              # 全局可复用组件
│   ├── bottom-popup/       # 底部弹窗基础组件
│   ├── navigation-bar/     # 导航栏组件
│   ├── *-detail-popup/     # 各类详情弹窗组件
│   └── ...
├── packageFinance/          # 财务管理分包
├── packageProduction/       # 生产管理分包
├── packageHealth/           # 健康管理分包
└── ...
```

### 1.2 文件命名

- **页面/组件文件**：使用 kebab-case（短横线命名）
  - ✅ `finance-record-list.ts`
  - ❌ `financeRecordList.ts`
  - ❌ `FinanceRecordList.ts`

- **类名/接口名**：使用 PascalCase（大驼峰）
  - ✅ `FinanceRecord`
  - ❌ `financeRecord`

- **变量/函数名**：使用 camelCase（小驼峰）
  - ✅ `selectedRecord`
  - ❌ `selected_record`

---

## 2. 组件化开发规范

### 2.1 组件设计原则

遵循以下设计原则进行组件开发：

#### **DRY 原则** (Don't Repeat Yourself)
- 当同样的 UI 或逻辑在 **2 个或更多** 地方使用时，必须提取为组件
- 避免复制粘贴代码

#### **单一职责原则**
- 每个组件只负责一个明确的功能
- 组件的修改只应该有一个原因

#### **高内聚，低耦合**
- 组件内部逻辑紧密相关
- 组件之间通过明确的接口（属性和事件）通信

### 2.2 组件创建时机

**必须创建组件的情况**：
- ✅ 同样的 UI 在 2+ 个页面使用
- ✅ 页面某个区块代码超过 100 行
- ✅ 功能需要在多处复用
- ✅ 便于单独测试和维护

**不需要创建组件的情况**：
- ❌ 只在一个地方使用的简单 UI
- ❌ 代码量很小（< 50 行）且无复用需求
- ❌ 与页面逻辑强耦合的代码

---

## 3. 详情弹窗组件复用规范 ⭐

### 3.1 规范说明

**适用场景**：当需要展示记录详情时（如财务记录、入栏记录、出栏记录等）

**强制要求**：
- ⚠️ **禁止**在页面中直接写详情弹窗的 UI 代码
- ⚠️ **必须**创建独立的详情弹窗组件
- ⚠️ **必须**遵循以下组件结构和命名规范

### 3.2 组件结构规范

#### 目录结构
```
components/
└── [module]-record-detail-popup/
    ├── [module]-record-detail-popup.json    # 组件配置
    ├── [module]-record-detail-popup.wxml    # 组件模板
    ├── [module]-record-detail-popup.ts      # 组件逻辑
    └── [module]-record-detail-popup.scss    # 组件样式
```

#### 命名规范
- 组件名：`[模块名]-record-detail-popup`
- 示例：
  - `finance-record-detail-popup` - 财务记录详情
  - `entry-record-detail-popup` - 入栏记录详情
  - `exit-record-detail-popup` - 出栏记录详情
  - `material-record-detail-popup` - 物料记录详情

### 3.3 组件实现模板

#### 1. 组件配置文件 (*.json)

```json
{
  "component": true,
  "usingComponents": {
    "bottom-popup": "/components/bottom-popup/bottom-popup",
    "t-tag": "tdesign-miniprogram/tag/tag"
  }
}
```

#### 2. 组件模板文件 (*.wxml)

```xml
<!-- [模块]记录详情弹窗组件 -->
<bottom-popup
  visible="{{visible}}"
  title="记录详情"
  show-close="{{true}}"
  show-actions="{{true}}"
  confirm-text="确定"
  bind:close="onClose"
  bind:confirm="onClose"
  bind:visiblechange="onVisibleChange"
>
  <view wx:if="{{record}}" class="record-detail-content">
    <view class="info-card">
      <view class="card-content">
        
        <!-- 必填字段 -->
        <view class="info-row">
          <text class="info-label">字段名：</text>
          <text class="info-value">{{record.fieldName}}</text>
        </view>
        <view class="divider-line"></view>
        
        <!-- 可选字段 -->
        <block wx:if="{{record.optionalField}}">
          <view class="info-row">
            <text class="info-label">可选字段：</text>
            <text class="info-value">{{record.optionalField}}</text>
          </view>
          <view class="divider-line"></view>
        </block>
        
        <!-- 状态字段 -->
        <view class="info-row">
          <text class="info-label">状态：</text>
          <view class="status-value">
            <t-tag theme="{{record.status === '已完成' ? 'success' : 'warning'}}" size="small">
              {{record.status}}
            </t-tag>
          </view>
        </view>
        
        <!-- 备注字段（可选） -->
        <block wx:if="{{record.notes}}">
          <view class="divider-line"></view>
          <view class="info-row">
            <text class="info-label">备注：</text>
            <text class="info-value note-text">{{record.notes}}</text>
          </view>
        </block>
        
      </view>
    </view>
  </view>
</bottom-popup>
```

#### 3. 组件逻辑文件 (*.ts)

```typescript
// components/[module]-record-detail-popup/[module]-record-detail-popup.ts
Component({
  options: {
    styleIsolation: 'apply-shared'
  },

  properties: {
    // 是否显示弹窗
    visible: {
      type: Boolean,
      value: false
    },
    // 记录数据
    record: {
      type: Object,
      value: null
    }
  },

  methods: {
    // 关闭弹窗
    onClose() {
      this.triggerEvent('close')
    },

    // 弹窗可见性变化
    onVisibleChange(e: any) {
      if (!e.detail.visible) {
        this.onClose()
      }
    }
  }
})
```

**重要说明**：
- ✅ 使用 `styleIsolation: 'apply-shared'` 允许页面样式覆盖组件样式
- ✅ 使用 `triggerEvent` 向父页面发送事件
- ✅ 通过 `properties` 接收父页面传递的数据

#### 4. 组件样式文件 (*.scss)

```scss
/* [模块]记录详情弹窗组件样式 */

/* 详情内容容器 */
.record-detail-content {
  padding: 0 !important;
  display: flex !important;
  flex-direction: column !important;
  gap: 0 !important;
}

/* 信息卡片 */
.record-detail-content .info-card {
  background: white !important;
  border-radius: 12rpx !important;
  padding: 20rpx !important;
  margin: 0 !important;
  box-shadow: none !important;
  border: 2rpx solid #0052d9 !important;
}

/* 卡片内容 */
.record-detail-content .card-content {
  display: flex !important;
  flex-direction: column !important;
  padding: 0 !important;
  overflow: visible !important;
}

/* 信息行 */
.record-detail-content .info-row {
  display: flex !important;
  justify-content: space-between !important;
  align-items: center !important;
  padding: 12rpx 0 !important;
  gap: 24rpx !important;
  margin-bottom: 0 !important;
}

/* 虚线分隔符 */
.record-detail-content .divider-line {
  height: 2rpx !important;
  min-height: 2rpx !important;
  width: 100% !important;
  border: none !important;
  border-top: 2rpx dashed #999 !important;
  margin: 10rpx 0 !important;
  padding: 0 !important;
  display: block !important;
}

/* 标签样式 */
.record-detail-content .info-label {
  font-size: 26rpx !important;
  color: #666 !important;
  flex-shrink: 0 !important;
  min-width: 120rpx !important;
  margin-right: 0 !important;
  font-weight: 400 !important;
}

/* 值样式 */
.record-detail-content .info-value {
  font-size: 28rpx !important;
  color: #333 !important;
  font-weight: 500 !important;
  text-align: right !important;
  flex: 1 !important;
  word-break: break-all !important;
  overflow-wrap: break-word !important;
  line-height: 1.5 !important;
}

/* 状态标签容器 */
.record-detail-content .status-value {
  display: flex !important;
  align-items: center !important;
  justify-content: flex-end !important;
  flex: 1 !important;
}

/* 备注文本 */
.record-detail-content .note-text {
  text-align: left !important;
  word-break: break-all !important;
  white-space: pre-wrap !important;
  line-height: 1.5 !important;
}
```

**样式规范说明**：
- ✅ 所有样式使用 `!important` 确保优先级
- ✅ 卡片使用白色背景 + 蓝色边框
- ✅ 标签固定宽度 120rpx，左对齐
- ✅ 值文本右对齐，自动换行
- ✅ 使用虚线分隔符（灰色虚线）
- ✅ 备注文本左对齐，支持多行

### 3.4 页面使用规范

#### 1. 在页面配置中引入组件

```json
{
  "usingComponents": {
    "[component-name]": "/components/[module]-record-detail-popup/[module]-record-detail-popup"
  }
}
```

**示例**：
```json
{
  "usingComponents": {
    "finance-record-detail": "/components/finance-record-detail-popup/finance-record-detail-popup"
  }
}
```

#### 2. 在页面模板中使用组件

```xml
<!-- 在页面底部添加 -->
<[component-name]
  visible="{{showDetailPopup}}"
  record="{{selectedRecord}}"
  bind:close="closeDetailPopup"
/>
```

**示例**：
```xml
<finance-record-detail
  visible="{{showDetailPopup}}"
  record="{{selectedRecord}}"
  bind:close="closeDetailPopup"
/>
```

#### 3. 在页面逻辑中控制显示

```typescript
// 页面 data
data: {
  showDetailPopup: false,      // 弹窗显示状态
  selectedRecord: null as any  // 选中的记录
}

// 查看记录详情
viewRecordDetail(e: any) {
  const { record } = e.currentTarget.dataset
  this.setData({
    selectedRecord: record,
    showDetailPopup: true
  })
}

// 关闭详情弹窗
closeDetailPopup() {
  this.setData({
    showDetailPopup: false
  })
  // ⚠️ 重要：延迟清空数据，避免弹窗关闭动画时数据闪烁
  setTimeout(() => {
    this.setData({
      selectedRecord: null
    })
  }, 300)
}
```

**关键点说明**：
- ✅ 使用 `setTimeout` 延迟 300ms 清空数据
- ✅ 延迟时间与弹窗关闭动画时间一致
- ✅ 避免关闭动画时出现数据闪烁

#### 4. 页面样式清理

创建组件后，**必须删除**页面中的详情弹窗相关样式：

```scss
// ❌ 删除这些样式（已移到组件中）
.record-detail-content { ... }
.info-card { ... }
.info-row { ... }
.divider-line { ... }
.info-label { ... }
.info-value { ... }
// ...
```

### 3.5 已实现的详情弹窗组件

以下组件已按照规范实现，可作为参考：

| 组件名 | 路径 | 用途 | 使用页面 |
|--------|------|------|----------|
| `finance-record-detail-popup` | `/components/finance-record-detail-popup/` | 财务记录详情 | 财务管理、财务记录列表 |
| `entry-record-detail-popup` | `/components/entry-record-detail-popup/` | 入栏记录详情 | 入栏记录列表 |
| `exit-record-detail-popup` | `/components/exit-record-detail-popup/` | 出栏记录详情 | 出栏记录列表 |
| `material-record-detail-popup` | `/components/material-record-detail-popup/` | 物料记录详情 | 物料记录列表 |

### 3.6 开发检查清单

在创建新的详情弹窗时，请确保：

- [ ] 组件命名符合规范 `[module]-record-detail-popup`
- [ ] 组件包含 4 个文件（json, wxml, ts, scss）
- [ ] 使用 `bottom-popup` 作为基础组件
- [ ] 实现 `visible` 和 `record` 属性
- [ ] 实现 `close` 事件
- [ ] 样式使用统一的设计规范
- [ ] 在页面配置中正确引入组件
- [ ] 在页面中正确使用组件
- [ ] 实现延迟清空数据机制
- [ ] 删除页面中的旧样式代码
- [ ] 测试弹窗显示、关闭动画正常

### 3.7 反模式（禁止做法）

❌ **禁止在页面中直接写详情弹窗代码**
```xml
<!-- ❌ 错误做法 -->
<bottom-popup visible="{{showDetailPopup}}">
  <view class="detail-content">
    <!-- 大量详情 UI 代码 -->
  </view>
</bottom-popup>
```

❌ **禁止复制粘贴弹窗代码到多个页面**
```
finance/finance.wxml          ← 有一份弹窗代码
finance-record-list.wxml      ← 又复制了一份
entry-records-list.wxml       ← 又复制了一份
❌ 这违反了 DRY 原则！
```

❌ **禁止不使用延迟清空数据**
```typescript
// ❌ 错误做法：立即清空数据会导致关闭动画时闪烁
closeDetailPopup() {
  this.setData({
    showDetailPopup: false,
    selectedRecord: null  // ❌ 不应该立即清空
  })
}
```

✅ **正确做法：创建独立组件**
```xml
<!-- ✅ 正确做法 -->
<finance-record-detail
  visible="{{showDetailPopup}}"
  record="{{selectedRecord}}"
  bind:close="closeDetailPopup"
/>
```

---

## 4. 命名规范

### 4.1 组件命名

- **全局组件**：使用描述性名称
  - `bottom-popup` - 底部弹窗
  - `navigation-bar` - 导航栏

- **功能组件**：使用 `[功能]-[类型]` 格式
  - `finance-record-detail-popup` - 财务记录详情弹窗
  - `entry-record-card` - 入栏记录卡片

### 4.2 变量命名

```typescript
// ✅ 好的命名
const selectedRecord = ...      // 选中的记录
const showDetailPopup = ...     // 显示详情弹窗
const isLoading = ...           // 是否加载中

// ❌ 不好的命名
const data = ...                // 太模糊
const flag = ...                // 含义不明确
const temp = ...                // 临时变量应该避免
```

### 4.3 函数命名

```typescript
// ✅ 好的命名（动词开头）
viewRecordDetail()              // 查看记录详情
closeDetailPopup()              // 关闭详情弹窗
loadRecords()                   // 加载记录

// ❌ 不好的命名
detail()                        // 不明确
popup()                         // 不明确
records()                       // 不明确
```

---

## 5. 样式规范

### 5.1 统一的设计系统

#### 颜色规范
```scss
// 主色调
$primary-color: #0052d9;        // 主题蓝
$success-color: #00a870;        // 成功绿
$warning-color: #ed7b2f;        // 警告橙
$error-color: #e34d59;          // 错误红

// 文本颜色
$text-primary: #333;            // 主要文本
$text-secondary: #666;          // 次要文本
$text-tertiary: #999;           // 辅助文本

// 背景颜色
$bg-primary: #ffffff;           // 主背景
$bg-secondary: #f5f5f5;         // 次背景
```

#### 间距规范
```scss
// 统一使用 8rpx 的倍数
$spacing-xs: 8rpx;              // 极小间距
$spacing-sm: 12rpx;             // 小间距
$spacing-md: 16rpx;             // 中等间距
$spacing-lg: 24rpx;             // 大间距
$spacing-xl: 32rpx;             // 超大间距
```

#### 圆角规范
```scss
$border-radius-sm: 8rpx;        // 小圆角
$border-radius-md: 12rpx;       // 中圆角
$border-radius-lg: 16rpx;       // 大圆角
```

### 5.2 样式隔离

组件必须使用 `styleIsolation` 控制样式隔离：

```typescript
Component({
  options: {
    styleIsolation: 'apply-shared'  // ✅ 允许页面样式影响组件
  }
})
```

### 5.3 Important 使用规范

- ✅ 组件样式可以使用 `!important` 确保优先级
- ❌ 页面样式尽量避免使用 `!important`

---

## 6. TypeScript 编码规范

### 6.1 类型定义

```typescript
// ✅ 好的做法：定义接口
interface FinanceRecord {
  id: string
  type: string
  amount: number
  date: string
  operator: string
  notes?: string  // 可选字段使用 ?
}

// ✅ 使用类型
const record: FinanceRecord = { ... }

// ❌ 避免使用 any
const data: any = { ... }
```

### 6.2 空值处理

```typescript
// ✅ 好的做法：明确处理空值
if (record && record.notes) {
  // 使用 notes
}

// 使用可选链
const amount = record?.rawRecord?.amount ?? 0

// ❌ 不好的做法：不检查就使用
const amount = record.rawRecord.amount
```

---

## 7. 数据交互规范

### 7.1 组件通信

#### 父 → 子（通过 properties）
```typescript
// 子组件定义
properties: {
  record: {
    type: Object,
    value: null
  }
}

// 父组件使用
<component record="{{selectedRecord}}" />
```

#### 子 → 父（通过 events）
```typescript
// 子组件触发
this.triggerEvent('close', { id: '123' })

// 父组件监听
<component bind:close="handleClose" />

// 父组件处理
handleClose(e: any) {
  const { id } = e.detail
}
```

### 7.2 数据请求

```typescript
// ✅ 统一使用 try-catch 处理错误
async loadData() {
  try {
    const result = await wx.cloud.callFunction({ ... })
    this.setData({ data: result.data })
  } catch (error) {
    console.error('加载数据失败', error)
    wx.showToast({
      title: '加载失败',
      icon: 'none'
    })
  }
}
```

---

## 8. 页面布局规范 ⭐

### 8.1 规范说明

**适用场景**：所有包含滚动列表的页面（如财务记录列表、报销列表、入栏记录列表等）

**强制要求**：
- ⚠️ **禁止**使用固定高度的 `scroll-view`
- ⚠️ **必须**使用 Flex 布局实现自适应高度
- ⚠️ **必须**最小化底部空白区域
- ⚠️ **必须**正确处理安全区域（safe-area）

### 8.2 标准页面布局结构

#### WXML 结构模板

```xml
<view class="page-container">
  <!-- 导航栏 -->
  <navigation-bar 
    title="页面标题"
    show-back="true"
  />

  <!-- 内容区域 -->
  <view class="content-wrapper">
    <!-- 筛选标签（可选） -->
    <view class="filter-tabs">
      <!-- 筛选标签内容 -->
    </view>

    <!-- 滚动列表 -->
    <scroll-view class="list-container" scroll-y>
      <!-- 列表内容 -->
      <view class="records-list">
        <!-- 列表项 -->
      </view>
      
      <!-- 底部安全区域占位 -->
      <view class="safe-area"></view>
    </scroll-view>
  </view>
</view>
```

### 8.3 SCSS 样式实现

#### 1. 页面容器样式

```scss
.page-container {
  height: 100vh;
  background-color: #f5f5f5;
  display: flex;
  flex-direction: column;
  overflow: hidden; /* 防止页面整体滚动 */
}
```

**关键点**：
- ✅ 使用 `height: 100vh` 而非 `min-height: 100vh`
- ✅ 必须设置 `overflow: hidden` 防止页面整体滚动
- ✅ 使用 `display: flex` 和 `flex-direction: column`

#### 2. 内容包装器样式

```scss
.content-wrapper {
  flex: 1;
  margin-top: calc(var(--status-bar-height, 44rpx) + var(--navbar-height, 88rpx) + 16rpx);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}
```

**关键点**：
- ✅ 使用 `flex: 1` 自适应高度
- ✅ 使用 CSS 变量计算导航栏高度：`var(--status-bar-height, 44rpx) + var(--navbar-height, 88rpx)`
- ✅ 添加 16rpx 的安全间距
- ✅ 必须设置 `overflow: hidden`

#### 3. 滚动容器样式

```scss
.list-container {
  flex: 1;
  padding: 16rpx 24rpx;
  box-sizing: border-box;
}
```

**关键点**：
- ✅ **禁止**使用固定高度：`height: calc(100vh - xxx)`
- ✅ **必须**使用 `flex: 1` 自适应高度
- ✅ **禁止**在 scroll-view 上设置 `padding-top`（由 content-wrapper 的 margin-top 处理）
- ✅ 只设置左右和内部的 padding

#### 4. 列表样式

```scss
.records-list {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
  padding-bottom: 0; /* 不设置底部padding，由safe-area处理 */
}
```

**关键点**：
- ✅ 使用 `gap` 控制列表项间距
- ✅ `padding-bottom: 0`，底部间距由 safe-area 处理

#### 5. 底部安全区域样式

```scss
.safe-area {
  height: constant(safe-area-inset-bottom);
  height: env(safe-area-inset-bottom);
  min-height: 0;
  flex-shrink: 0;
}
```

**关键点**：
- ✅ 只保留必要的安全区域高度，不添加额外 padding
- ✅ 使用 `constant()` 和 `env()` 兼容不同设备
- ✅ 设置 `flex-shrink: 0` 防止被压缩

#### 6. 卡片间距优化

```scss
.card:last-child {
  margin-bottom: 0; /* 最后一张卡片不需要底部间距，由safe-area处理 */
}

.scroll-container > .card:first-child {
  margin-top: 0; /* 第一个卡片顶部不需要额外间距 */
}
```

### 8.4 完整示例

#### 示例：财务记录列表页面

**WXML** (`finance-record-list.wxml`)：
```xml
<view class="page-container">
  <navigation-bar 
    title="财务记录"
    show-back="true"
  />

  <view class="content-wrapper">
    <view class="filter-tabs">
      <!-- 筛选标签 -->
    </view>

    <scroll-view class="list-container" scroll-y>
      <view class="records-list">
        <!-- 列表项 -->
      </view>
      
      <view class="safe-area"></view>
    </scroll-view>
  </view>
</view>
```

**SCSS** (`finance-record-list.scss`)：
```scss
.page-container {
  height: 100vh;
  background-color: #f5f5f5;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.content-wrapper {
  flex: 1;
  margin-top: calc(var(--status-bar-height, 44rpx) + var(--navbar-height, 88rpx) + 16rpx);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.filter-tabs {
  display: flex;
  background: white;
  padding: 16rpx 32rpx 12rpx 32rpx;
  flex-shrink: 0;
  border-bottom: 1rpx solid #e5e7eb;
}

.list-container {
  flex: 1;
  padding: 16rpx 24rpx;
  box-sizing: border-box;
}

.records-list {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
  padding-bottom: 0;
}

.safe-area {
  height: constant(safe-area-inset-bottom);
  height: env(safe-area-inset-bottom);
  min-height: 0;
  flex-shrink: 0;
}
```

### 8.5 反模式（禁止做法）

#### ❌ 禁止使用固定高度

```scss
/* ❌ 错误做法 */
.scroll-container {
  height: calc(100vh - 88rpx); /* 固定高度会导致底部空白过大 */
  padding-top: calc(88rpx + 84rpx);
}
```

#### ❌ 禁止在 scroll-view 上设置 padding-top

```scss
/* ❌ 错误做法 */
.scroll-container {
  padding-top: calc(88rpx + 84rpx); /* 应该由 content-wrapper 的 margin-top 处理 */
}
```

#### ❌ 禁止在列表上设置过大的 padding-bottom

```scss
/* ❌ 错误做法 */
.records-list {
  padding-bottom: 32rpx; /* 过大的底部padding会导致空白过大 */
}
```

#### ❌ 禁止在最后一个卡片上设置 margin-bottom

```scss
/* ❌ 错误做法 */
.card:last-child {
  margin-bottom: 32rpx; /* 应该由 safe-area 处理 */
}
```

### 8.6 已优化的页面

以下页面已按照规范实现，可作为参考：

| 页面名 | 路径 | 说明 |
|--------|------|------|
| 财务记录列表 | `/packageFinance/finance-record-list/` | 使用 Flex 布局，最小化底部空白 |
| 财务管理主页面 | `/packageFinance/finance/` | 使用 Flex 布局，最小化底部空白 |
| 报销列表 | `/packageFinance/reimbursement-list/` | 使用 Flex 布局，最小化底部空白 |

### 8.7 开发检查清单

在创建或修改页面布局时，请确保：

- [ ] 页面容器使用 `height: 100vh` 和 `display: flex`
- [ ] 页面容器设置 `overflow: hidden`
- [ ] 使用 `content-wrapper` 作为内容包装器
- [ ] `content-wrapper` 使用 `flex: 1` 和正确的 `margin-top`
- [ ] `scroll-view` 使用 `flex: 1` 而非固定高度
- [ ] 列表设置 `padding-bottom: 0`
- [ ] 最后一个卡片设置 `margin-bottom: 0`
- [ ] 添加 `safe-area` 占位元素
- [ ] `safe-area` 只保留必要的安全区域高度
- [ ] 测试在不同设备上的显示效果

### 8.8 常见问题

#### Q: 为什么不能使用固定高度？

A: 固定高度（如 `height: calc(100vh - 88rpx)`）会导致：
- 底部空白过大
- 在不同设备上显示不一致
- 无法正确处理安全区域

#### Q: 为什么需要 content-wrapper？

A: `content-wrapper` 的作用：
- 统一处理导航栏高度计算
- 提供统一的布局结构
- 便于维护和修改

#### Q: safe-area 的高度如何确定？

A: `safe-area` 的高度：
- 使用 `env(safe-area-inset-bottom)` 自动获取设备安全区域高度
- 不添加额外的 padding，只保留必要的安全区域
- 在非刘海屏设备上高度为 0

---

## 9. 合规与上线要求

### 9.1 微信小程序主包/分包规范
- 主包需控制在 2MB 以内，主包 + 分包总体不超过 30MB（服务商代开发不超过 20MB），超出必须拆分功能模块。
- `tabBar` 页面必须驻留在主包，分包资源不可直接互相引用；公共依赖需放入主包或启用分包异步化能力。
- 通过 `app.json` 的 `subPackages` 与 `preloadRule` 精细声明分包根目录、入口文件及预下载策略，确保首屏性能与按需加载体验。

### 9.2 云开发最佳实践
- 云函数保持单一职责，合理设置超时时间与并发调用，敏感操作补充审计日志（参考 `health-management`、`breeding-todo` 云函数）。
- 云数据库集合字段需扁平化、索引化，避免文档过大；读写频繁的统计字段应由云函数聚合返回，前端只做展示。
- 权限校验统一由云函数侧完成，所有前端调用需通过 `CloudApi.callFunction` 封装，确保错误统一处理与用户提示一致。

### 9.3 TDesign 组件规范
- 组件按需声明于页面/组件 `*.json` 的 `usingComponents`，禁止在 WXML 中直接引用未注册的 TDesign 组件。
- 自定义样式优先使用官方提供的外部样式类（如 `t-class`, `t-class-loading` 等）覆盖状态，自定义主题色通过 CSS 变量（示例：`--td-rate-selected-color`）统一维护。
- 严禁改写 TDesign 组件内部结构，扩展需求通过插槽、受控属性或组合封装实现，保持升级兼容性。

### 9.4 日志与调试策略
- 生产环境禁止直接输出 `console.log`，调试日志需通过 `process.env.DEBUG_LOG === 'true'` 控制的 `debugLog` 方法输出。
- 线上故障排查优先使用云函数高级日志、云监控告警；本地调试开启 `DEBUG_LOG` 后记得在发布前关闭。

### 9.5 数据一致性与页面跳转
- 前端页面通过 `CloudApi` 获取数据后统一写入页面 `data`，禁止跨页面持久化未验证的数据；任务、健康、财务模块均需在返回结果后校验 `success` 标识。
- 所有页面跳转统一使用 `wx.navigateTo` / `wx.switchTab`，并在跳转前校验必需参数；从分包返回首页需同步 `globalData.needSyncHomepage`，避免数据残留。

---

## 📋 开发流程检查清单

### 新增功能时

- [ ] 检查是否有可复用的组件
- [ ] 如果有重复代码，考虑提取为组件
- [ ] 遵循命名规范
- [ ] 遵循样式规范
- [ ] 遵循页面布局规范
- [ ] 添加必要的类型定义
- [ ] 添加错误处理
- [ ] 测试各种场景

### 创建详情弹窗时

- [ ] 使用 `[module]-record-detail-popup` 命名
- [ ] 创建完整的 4 个文件
- [ ] 使用统一的样式规范
- [ ] 实现延迟清空数据机制
- [ ] 删除页面中的旧代码
- [ ] 测试弹窗动画效果

### 代码审查时

- [ ] 检查是否有重复代码
- [ ] 检查命名是否规范
- [ ] 检查样式是否统一
- [ ] 检查页面布局是否符合规范
- [ ] 检查底部空白是否最小化
- [ ] 检查类型定义是否完整
- [ ] 检查错误处理是否完善

---

## 📚 参考文档

- 历史专题、重构案例与排障手册已归档至 `docs/archive/`，可按关键词检索获取详情。

---

## ⚠️ 重要提醒

1. **遇到重复代码时，立即重构**
   - 不要"先实现功能，以后再优化"
   - 技术债会越积越多

2. **遵循现有规范**
   - 查看已有组件的实现方式
   - 保持代码风格一致

3. **组件优先**
   - 优先考虑使用现有组件
   - 如需新功能，优先扩展现有组件而非创建新组件

4. **定期重构**
   - 发现重复代码时立即重构
   - 不要等到技术债积累太多

---

## 📞 联系方式

如有疑问或建议，请联系：
- 项目负责人：[待补充]
- 技术支持：[待补充]

---

**最后更新**: 2025年11月9日  
**文档版本**: v1.2

### 更新日志

- **2025-11-09**: 补充微信小程序分包、云开发、TDesign 组件及日志合规要求；统一调试日志策略。
- **2025-11-07**: v1.1 发布，完善组件复用流程与页面布局规范。

