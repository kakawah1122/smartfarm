/* ==================== 导航栏适配通用样式 ==================== */
/* 此文件用于修复自定义导航栏遮挡页面内容的问题 */

/* 定义导航栏总高度变量 */
:root {
  --navbar-total-height: calc(44px + 44px); /* 状态栏(44px) + 导航栏(44px) */
}

/* 为所有使用自定义导航栏的页面容器添加顶部间距 */
.page-with-navbar {
  padding-top: var(--navbar-total-height);
}

/* 滚动视图容器 */
.scroll-view-with-navbar {
  padding-top: var(--navbar-total-height);
}

/* 主内容区域 */
.main-content-with-navbar {
  margin-top: var(--navbar-total-height);
}

